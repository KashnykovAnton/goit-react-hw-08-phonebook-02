import styles from 'Pages/NotFoundPage/NotFoundPage.module.css';
const NotFoundPage = () => {
  return (
    <h1 className={styles.title}>Unfortunately the page does not exist!</h1>
  );
};

export default NotFoundPage;
